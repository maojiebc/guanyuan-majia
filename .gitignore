# 凭据 — 绝对不能上传 / Credentials — never commit
config.json
.config.local.json

# Deny-list 本身含禁词，禁止 commit 到 public repo
# Maintained locally; consumed by majia-ota-skill audit_skill_release.py --pii-scan
.ota-deny-list.txt

# 缓存数据 / Cache data
.cache/
data/columns_cache/

# Local dual-agent sync lock (per-machine; not for distribution)
.skill-sync.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# vim swap files
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Editor
.vscode/
.idea/
*.iml
